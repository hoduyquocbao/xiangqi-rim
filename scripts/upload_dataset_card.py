# ============================================================================
# XIANGQI-RIM HUGGING FACE DATASET CARD & MODEL CARD AUTOMATIC GENERATOR
# ============================================================================
# Tự động tạo và cập nhật tệp README.md (Dataset Card & Model Card với YAML Frontmatter chuẩn)
# lên Hugging Face Hub repos:
#   - Dataset Repo: `hoduyquocbao/xiangqi-nnue-dataset`
#   - Model Repo:   `hoduyquocbao/xiangqi-rim`
# Kích hoạt tính năng Hugging Face Dataset Viewer tự động hiển thị dữ liệu!
# ============================================================================

import os
import sys

sys.path.insert(0, os.getcwd())

from huggingface_hub import HfApi

def upload_cards():
    token = os.environ.get("HF_TOKEN")
    if not token:
        print("❌ Thiếu HF_TOKEN trong môi trường!")
        return

    api = HfApi(token=token)
    user_info = api.whoami()
    username = user_info['name']

    repo_dataset = f"{username}/xiangqi-nnue-dataset"
    repo_model = f"{username}/xiangqi-rim"

    dataset_readme = """---
annotations_creators:
- expert-generated
language:
- zh
- en
license: mit
multilinguality:
- multilingual
size_categories:
- 100M<n<1B
source_datasets:
- original
task_categories:
- reinforcement-learning
- tabular-classification
pretty_name: Xiangqi-RIM 1 Billion Native Rust Engine NNUE Dataset
configs:
- config_name: default
  data_files:
  - split: train
    path: "chunks/*.parquet"
dataset_info:
  features:
  - name: fen
    dtype: string
  - name: best_move
    dtype: string
  - name: score
    dtype: int32
  - name: depth
    dtype: int32
---

# 🪓 Xiangqi-RIM 1 Billion Native Rust Engine NNUE Dataset

High-performance native Rust engine generated dataset containing **1,000,000,000 (1 Billion)** Chinese Chess (Xiangqi) positions, UCI best moves, centipawn evaluations, and search depths stored in **Apache Parquet (`.parquet`)** format.

## 📊 Dataset Structure
Each chunk in `chunks/chunk_XXX_10m.parquet` contains 10,000,000 FEN samples compressed with Snappy Parquet (file size ~190 MB per 10M FENs):

```json
{"fen":"rnbakabnr/9/1c5c1/p1p1p1p1p/9/9/P1P1P1P1P/1C5C1/9/RNBAKABNR w - - 0 1","best_move":"h2e2","score":15,"depth":4}
```

## ⚙️ Fields Specification
- `fen` (string): Complete 10-rank Xiangqi FEN string.
- `best_move` (string): 4-character UCI format move string (e.g., `h2e2`).
- `score` (int32): Centipawn evaluation score from 100% Native Rust Engine.
- `depth` (int32): Playout search depth (Depth 4+).

## 🚀 Native Engine Acceleration
Generated by 100% Native Rust SIMD & physical CPU cache-line aligned ultra-miner (`20_parallel_mine.rs` / `31_vram_direct_pipeline.rs`) at **159,180 FEN/sec (9.55 Million FEN/min)** throughput.
"""

    model_readme = """---
language:
- zh
- en
license: mit
pipeline_tag: reinforcement-learning
tags:
- xiangqi
- nnue
- rust
- chess-engine
library_name: PyTorch
pretty_name: Xiangqi-RIM 1 Billion FEN NNUE Model Weights
---

# 🤖 Xiangqi-RIM 1 Billion FEN NNUE Model Weights (XRNN v1)

Binary model weights (`nnue_weights_1b_latest.bin`) trained on **1 BILLION FENs** using 100% Native Rust Engine self-play evaluations and PyTorch CUDA acceleration.

## 🧠 Architecture Specification (XRNN v1)
- **Feature Transformer**: HalfKAv2_hm ($65,536 \\times 256 \\to 512 \\to 32 \\to 1$)
- **File Size**: 33,571,504 Bytes (32.02 MB)
- **Format**: Quantized Int16 / Int8 XRNN v1 Binary Matrix

## ⚡ Loading in Rust Engine
```rust
use xiangrust::eval::nnue::Nnue;

let nnue = Nnue::load("data/nnue_weights_1b_latest.bin").expect("Loaded NNUE weights");
```
"""

    print("--> Uploading Dataset Card README.md to Hugging Face Hub...", flush=True)
    api.upload_file(
        path_or_fileobj=dataset_readme.encode('utf-8'),
        path_in_repo="README.md",
        repo_id=repo_dataset,
        repo_type="dataset"
    )
    print("✅ Dataset Card uploaded successfully!", flush=True)

    print("--> Uploading Model Card README.md to Hugging Face Hub...", flush=True)
    api.upload_file(
        path_or_fileobj=model_readme.encode('utf-8'),
        path_in_repo="README.md",
        repo_id=repo_model,
        repo_type="model"
    )
    print("✅ Model Card uploaded successfully!", flush=True)

if __name__ == "__main__":
    upload_cards()
